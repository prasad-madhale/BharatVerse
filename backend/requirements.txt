# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
supabase==2.9.0  # Supabase Python client (includes postgrest and httpx dependencies)

# Data validation and serialization
pydantic>=2.10.0
pydantic-settings>=2.6.0
email-validator==2.3.0      # Required for pydantic's EmailStr (used in auth request models)

# LLM and AI
google-generativeai==0.3.2  # Google Gemini (FREE - default)
anthropic==0.116.0          # Anthropic Claude (optional) -- 0.18.1 predates current model IDs
openai>=1.55.3              # OpenAI (optional) -- must match scrapper/requirements.txt's floor
                             # (crawl4ai's litellm dependency), since CI installs both into
                             # the same environment sequentially
groq==0.4.1                 # Groq (optional)

# Environment and configuration
python-dotenv==1.0.1

# Note: no JWT/password-hashing libraries here -- authentication is handled
# entirely by Supabase Auth (see backend/database/schema.sql and design.md).

# Testing
pytest==7.4.4
pytest-asyncio==0.23.4
pytest-rerunfailures==14.0
pytest-cov==7.1.0
hypothesis==6.98.3

# Code quality
autopep8==2.0.4
flake8==7.0.0

# Utilities
python-dateutil==2.8.2
numpy==1.26.3
