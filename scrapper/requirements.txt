# Core dependencies
python-dotenv==1.0.1
pydantic>=2.10.0  # Required by crawl4ai
pydantic-settings>=2.6.0  # Used by common/config.py (shared LLM settings)

# Web scraping
crawl4ai==0.4.24

# Content source APIs
wikipedia==1.4.0  # Wikipedia search and API wrapper
internetarchive  # Archive.org search and API wrapper

# LLM providers (used via common/llm_provider.py)
google-generativeai==0.3.2  # Google Gemini (FREE - default)
anthropic==0.116.0          # Anthropic Claude (optional) -- 0.18.1 predates current model IDs
openai>=1.55.3               # OpenAI (optional) -- floor set by crawl4ai's litellm dependency
groq==0.4.1                  # Groq (optional)
json-repair==0.61.2          # Tolerates the malformed JSON LLMs commonly produce in prose responses

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==7.1.0

# Code quality
autopep8>=2.1.0  # floor, not exact -- 2.0.x imports the stdlib lib2to3 module, removed in Python 3.13
flake8==7.0.0
